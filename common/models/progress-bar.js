module.exports = function(ProgressBar) {
  // ProgressBar.afterRemote('**', function (ctx, result, next) {
  //   if(ctx.result) {
  //     if(Array.isArray(ctx.result)) {
  //       ctx.result = { 'progressBar': result };
  //     } else {
  //       ctx.result = { 'progressBar': result };
  //     }
  //   }

  //   next();
  // });

  // ProgressBar.observe('before validate', function (ctx, next) {
  //   ctx.instance = ctx.instance.progressBar;
  //   console.log(ctx.instance);
  //   next();
  // });
};
